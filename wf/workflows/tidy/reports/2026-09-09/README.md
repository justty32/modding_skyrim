# 2026-09-09 公司端資料夾整理

Done when: 136 份研究各有唯一分類、兩份歷史日誌已封存、入口可找到下一層、搬移內容保留、連結與離線測試通過、變更已 commit 並 push。

## 整理範圍

根目錄保留總入口；開發專案、研究領域與獨立 patch 各有自己的 README。
`analysis/mod-survey/findings/` 依原有索引分成七類，檔名與研究結論不變。
兩份已完成的分析操作日誌移到所屬領域的封存區。
已完成的第三方 mod 流水線改列結案，母 repo 的進度頁只留未完成工作。

完整搬移對照在 [moves.tsv](moves.tsv)，共 138 筆；每列是舊路徑與新路徑，以母 repo 為基準。
沒有把同類的原始抽取物拆散，也沒有改遊戲設定、呼叫 MongoDB 或重新判定實機驗收。

## 子 repo

15 個子 repo 都盤點了文件入口與用途分類。這次改了七個：`agentctl`、`instance`、
`mod-library`、`modpack-design`、`projects/ModForge`、`projects/my_skyrim_plugin_1`、
`projects/scene-capture-bridge`。修改包含補導航、把首頁細節移到下一層，以及修跨 repo 連結。
其他八個的文件已有入口或同類集中，保留現有位置；沒有為了改目錄而搬程式碼。

公司端先接上遠端的 115 個母 repo 提交，再對齊九個子 repo 與 `instance/profiles` 的已發布版本。
版本同步只更新 checkout，沒有執行部署工具或資料庫操作。

## 驗證

- 136 份研究逐份與搬移前 Git 內容比對：搬移後原文相同；修鏈後僅連結目的地有變。
- 138 筆搬移的舊路徑均消失、新路徑均存在；七類入口與總入口都少於 100 行。
- 母 repo 工具測試：44 個通過。公司 checkout 的 Git hook 先以本機 LF 換行執行，沒有提交換行噪音。
- 28 份子 repo 變動文件的 292 條連結通過；另檢查 plugin 答案入口與修正報告的 54 條連結通過。
- 完整連結盤點先發現 88 條既存壞鏈：正式報告 15 條已修；另外 73 條均在必須原樣保存的 inbox 原件。
  驗證排除 `agentctl/inbox/done/*/originals/*`，不排除正式報告或現役文件；CLI 與 CI 範圍已同步。
- `wf-lint --strict wf` 的連結、大小、模板殘留與查詢均為零；既有大表提示保留，不把資料格式重寫混入本次分類。
  全目錄 lint 還會掃到未版控的外部調查 clone；原始材料與完整研究筆記依結構原則保留，不為門檻硬拆。

最終全庫連結：4,209 份文件、4,131 條連結通過；291 份原樣訊息副本明確排除。
七個改動子 repo 已各自提交並推送，母 repo 最後提交對應 gitlink。

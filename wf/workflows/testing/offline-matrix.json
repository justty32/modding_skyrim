{
 "contract": "wf-table/1",
 "source": "wf/workflows/testing/offline-matrix.md",
 "extracted": "2026-08-30",
 "columns": [
  "repo",
  "命令",
  "本機基線／範圍"
 ],
 "rows": [
  {
   "repo": "`ModForge`",
   "命令": "`./scripts/test-offline.sh`",
   "本機基線／範圍": "1190 pass；排除 `RequiresSkyrim`"
  },
  {
   "repo": "`agent-bridge`",
   "命令": "`PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s client -p 'test*.py' -v`",
   "本機基線／範圍": "88 pass；stdlib-only Linux client、無需 MO2/遊戲"
  },
  {
   "repo": "`game-data`",
   "命令": "`python -m unittest discover -s tests -v`",
   "本機基線／範圍": "12 pass；fake dotnet，無需遊戲資料"
  },
  {
   "repo": "`skyrim-voicegen`",
   "命令": "`python -m unittest discover -s tests -v`",
   "本機基線／範圍": "6 pass；不載 TTS 模型"
  },
  {
   "repo": "`model-converter`",
   "命令": "`.venv/bin/python -m pytest`",
   "本機基線／範圍": "68 pass；若無 POSIX venv，依該 repo README 建環境"
  },
  {
   "repo": "`model-converter` (Windows)",
   "命令": "`.venv\\Scripts\\python.exe -m pytest`",
   "本機基線／範圍": "68 pass；Windows 等價命令"
  },
  {
   "repo": "`darksouls-port`",
   "命令": "`PYTHONDONTWRITEBYTECODE=1 ./venv/bin/python -m unittest discover -s tests -p \"test_*.py\" -v`",
   "本機基線／範圍": "35 pass；**測試在 `tests/` 不是 `tools/`，且必須用 repo 自帶的 `venv/`**——系統 Python 缺 numpy，會變成 2 error / 19 skip"
  },
  {
   "repo": "`scene-capture-bridge`",
   "命令": "`ctest --test-dir build/tests-native --output-on-failure`",
   "本機基線／範圍": "2 pass（Linux native）。`build/portable-tests-mingw` 是 **Windows MinGW** 的目錄，這台機器上不存在；完整 x64 triplet 尚缺"
  },
  {
   "repo": "`my_skyrim_plugin_1`",
   "命令": "`./scripts/test_quest_prf.sh`",
   "本機基線／範圍": "25 pass；quest PRF primitives，純 stdlib g++，不需 SKSE／CommonLib／Windows。Windows 對等物是 `scripts/test_quest_prf.ps1`"
  },
  {
   "repo": "`my_skyrim_plugin_1`",
   "命令": "`./scripts/test_packaging.sh`",
   "本機基線／範圍": "10 pass；`pack.sh` 的打包契約（zip 內路徑佈局、`--output-dir` 防護、CLI exit code）。Windows 對等物 `test_packaging.ps1` 測的是 `pack.ps1`，兩支打包腳本各自獨立"
  },
  {
   "repo": "`godot-worldspace-editor`",
   "命令": "`python tests/test_placements_contract.py`",
   "本機基線／範圍": "source gate 必跑；缺 Godot 時 runtime 明示 skip"
  },
  {
   "repo": "`godot-worldspace-editor`",
   "命令": "`python tests/test_model_fetch_contract.py`",
   "本機基線／範圍": "model-converter→Godot live contract；缺 Godot時 skip"
  },
  {
   "repo": "母 repo `tools/`",
   "命令": "`python3 -m unittest discover -s tools -p 'test_*.py'`",
   "本機基線／範圍": "36 pass；工作區自己的檢查工具（submodule pin guard、markdown 連結、code-map 覆蓋率）。純 stdlib，不需 DB 也不需遊戲"
  },
  {
   "repo": "`agentctl/tools/`",
   "命令": "`python3 -m unittest discover -s agentctl/tools -p 'test_*.py'`",
   "本機基線／範圍": "46 pass（2026-08-26 從 14 增加）；離線，不連網不連 Mongo。這批測的是 agent 驅動用的判斷邏輯，紅燈代表判斷本身壞了"
  },
  {
   "repo": "`mod-library/db/`",
   "命令": "從 `mod-library/db/` 執行 `python3 -m unittest discover -s . -p 'test_*.py'`",
   "本機基線／範圍": "36 pass；路徑契約與 intake 檢查。**必須 cd 進該目錄**，測試是用相對路徑載同目錄腳本的"
  },
  {
   "repo": "`instance/tools/`",
   "命令": "從 `instance/tools/` 執行 `python3 -m unittest discover -s . -p 'test_*.py'`",
   "本機基線／範圍": "3 pass。`instance/profiles/tools/` 目前 **0 個測試**——那裡的 profile 結構稽核沒有任何測試，是已知缺口"
  },
  {
   "repo": "`houseCARL`",
   "命令": "`nice -n 19 taskset -c 8-13 dotnet build housecarl.sln`",
   "本機基線／範圍": "exit 0；18 warning／0 error，約 4s。**沒有測試專案**——`dotnet test` 會 exit 0 並發現 0 個專案，那個綠燈沒有意義，別當閘門"
  },
  {
   "repo": "`houseCARL`",
   "命令": "`dotnet publish src/housecarl-generator -c Debug -r linux-x64 --self-contained true -o /tmp/hc-gen` ＋ `/tmp/hc-gen/housecarl-generator ci-all`",
   "本機基線／範圍": "**78/97 pass、19 fail、9 self-SKIP**，約 16s。19 條全是 Linux 移植缺口（`\\` 當路徑分隔 12、Windows 檔案鎖語意 4、`where.exe`／`.exe` 查找 3），非產品回歸。**必須 self-contained publish**——本機無 .NET 9／ASP.NET Core 9 runtime，framework-dependent 直接 exit 150"
  },
  {
   "repo": "`houseCARL`",
   "命令": "`/tmp/hc-gen/housecarl-generator freshness-capture-guard`（沿用上一列的 publish）",
   "本機基線／範圍": "ALL PASS、exit 0。CI 刻意另開冷行程跑這條（deferral arm 計時敏感），別併進 `ci-all`"
  }
 ]
}

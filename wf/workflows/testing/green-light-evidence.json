{
 "contract": "wf-table/1",
 "source": "wf/workflows/testing/green-light-evidence.md",
 "extracted": "2026-08-30",
 "columns": [
  "檢查",
  "為什麼恆真"
 ],
 "rows": [
  {
   "檢查": "profile 結構稽核（歷史）",
   "為什麼恆真": "只看 profile 目錄、不看 `ModOrganizer.ini`，讓不存在的 profile `PandoraRuntimeDefer-20260822` 每次都 PASS；2026-08-23 的 `instance/profiles` commit `241522d`（`feat(tools): validate selected_profile against the canonical name`）新增 `selected_profile_errors()`，讀 `ModOrganizer.ini` 的 `selected_profile=`，與 `CANONICAL_PROFILE` 不符就擋下"
  },
  {
   "檢查": "teardown 的「遊戲鎖已釋放」",
   "為什麼恆真": "鎖的路徑指向已被刪除的 `~/skyrim_agent_out/_lock/`——**檢查一個不可能存在的東西，永遠會過**"
  },
  {
   "檢查": "`check_markdown_links.py`（歷史）",
   "為什麼恆真": "`git ls-files` 到 gitlink 就停，漏看四條線的 87 個壞連結；也只驗檔案存在、不驗 `#anchor`。**兩者都已修（gitlink；anchor 於 `26dd4f7`）。2026-08-26 以突變測試複驗，實作會變紅，但測試有四個空隙（標題內 inline link、anchor 側 fenced code、closed ATX 尾綴、一行死碼），已於 `76df55f` 補齊，13/13 突變體全殺。**"
  },
  {
   "檢查": "自製的 CJK 偵測",
   "為什麼恆真": "`b.decode('utf-8', errors='ignore')` **永遠不拋錯**，所以「依序試多種編碼、成功就 break」的迴圈第一輪就結束，根本沒試過 cp936"
  }
 ]
}

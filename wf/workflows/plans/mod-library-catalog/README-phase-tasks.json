{
 "contract": "wf-table/1",
 "source": "README.md",
 "extracted": "2026-09-25",
 "columns": [
  "#",
  "任務",
  "驗證"
 ],
 "rows": [
  {
   "#": "1.1",
   "任務": "掃描器：走 `~/skyrim_mods/`，算 sha256、讀目錄表、解析檔名 → `archives`（upsert，D2 分離）",
   "驗證": "1,693 筆全進；`--dry-run` 只印不寫；重跑一次養成欄位不變"
  },
  {
   "#": "1.2",
   "任務": "`mods` 聚合 + L1 重複偵測",
   "驗證": "根目錄那 25 個 bit-identical 檔正確被認出"
  },
  {
   "#": "1.3",
   "任務": "DLL runtime 檢查（解 dll → 解析 `SKSEPlugin_Version` → `dll_compat` / `runtime_ok_1_6_1170`）",
   "驗證": "抽驗 `SmoothCam`（多 runtime）判為 OK；抽一個純 SE 期插件判為不 OK"
  },
  {
   "#": "1.4",
   "任務": "Nexus 補值：`housecarl_nexus_mod` 查在架狀態與最新版 → `nexus_status` / `never_delete`",
   "驗證": "抽驗一個已下架 mod 正確標成 `gone` 且 `never_delete`"
  },
  {
   "#": "1.5",
   "任務": "清理報告產生器（四級分類 + L2 三條例外）",
   "驗證": "報告可重跑且結果一致；例外規則有測到"
  },
  {
   "#": "1.6",
   "任務": "隔離器（移動 + 回寫 Mongo，非刪除）",
   "驗證": "移動後可用記錄完整還原；`--dry-run` 先出清單"
  },
  {
   "#": "1.7",
   "任務": "備份：pymongo dump 到 json（機器無 `mongodump`，同 RimWorld）",
   "驗證": "任何清理動作前自動先 dump"
  }
 ]
}

# --- 他人的 clone 與二進位素材:不進版控 ---------------------------------
# 著作權(不是我們的產物)+ 體積(合計約 490MB)+ 它們各自都有自己的 .git。
# 需要時照各自 README 重新 clone。
/external/frameworks/
/analysis/tool-survey/repos/

# --- 本機環境 ---------------------------------------------------------
venv/
.venv/
__pycache__/
*.pyc
